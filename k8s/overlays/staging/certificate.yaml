apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: projectnowcdp-report-studio-staging-tls
spec:
  secretName: projectnowcdp-report-studio-staging-tls
  issuerRef:
    name: hetzner-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - "*.staging-report.projectnowcdp.com"
