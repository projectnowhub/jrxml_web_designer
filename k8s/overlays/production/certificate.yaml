apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: projectnowcdp-report-studio-production-tls
spec:
  secretName: projectnowcdp-report-studio-production-tls
  issuerRef:
    name: hetzner-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - "*.report.projectnowcdp.com"
